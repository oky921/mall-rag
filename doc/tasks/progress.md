# 实施进度

- [x] 梳理现有 Spring Boot 与 React 结构。
- [x] 确认本机 MySQL 8.0.31 和连接凭据。
- [x] 实现商城后端与测试。
- [x] 实现商城前端与界面切换。
- [x] 完成构建和运行验证。

## 记录

- 保留现有 RAG 相关未提交改动，不在商城任务中修改。
- `mvn -q -DskipTests compile` 通过。
- `mvn test` 通过：23 个测试，0 失败。
- `npm run build` 通过。
- MySQL 8.0.31 实际联调通过，商品接口返回 8 条数据、4 个分类。
- Edge 桌面与手机尺寸截图验证完成。

## 商城二期

- [x] 梳理一期数据模型、API、前端路由与构建方式。
- [x] 固定二期接口契约和下单事务边界。
- [x] 实现 SKU 与商品详情。
- [x] 实现数据库购物车。
- [x] 实现模拟订单、库存扣减与购物车清理。
- [x] 实现详情、结算和订单结果前端。
- [x] 完成自动测试、构建和真实环境联调。

## 商城二期验证记录

- `mvn -q test` 通过：27 个测试，0 失败。
- `npm run build` 通过。
- MySQL 8.0.31 实际联调通过：商品详情返回 SKU，购物车新增/改量成功，订单创建后库存扣减且对应购物车项删除。
- 联调订单 ID 为 `1`，状态 `CREATED`，2 件商品总金额 `8598.00`。
- Edge 无头模式完成 1440x1000 商品详情与 390x844 手机结算页视觉检查。
- 外部模型 API Key 未写入文件，也未用于本次商城联调。

## 商城三期：订单中心

- [x] 固定订单状态机、接口和库存回补事务边界。
- [x] 实现订单列表与模拟支付。
- [x] 实现取消订单与库存回补。
- [x] 补充状态机和幂等测试。
- [x] 实现订单中心前端。
- [x] 完成构建、联调和视觉检查。

## 商城三期验证记录

- `mvn -q test` 通过：32 个测试，0 失败。
- `npm run build` 通过。
- MySQL 实际联调通过：重复支付流水一致，首次取消回补库存，重复取消净库存变化为 0。
- 已支付订单取消返回 HTTP 409；订单状态筛选返回正确结果。
- 修复全局异常处理器吞掉 `ResponseStatusException`、错误返回 500 的问题，并增加回归测试。
- Edge 无头模式完成 1440x1000 订单中心和 390x844 已支付订单详情视觉检查。
# 商城第四阶段：用户认证、个人中心与收货地址管理

- [x] 固化阶段范围、认证边界、数据模型与 API 契约。
- [x] 实现 Spring Security、用户注册登录和 HttpOnly Session。
- [x] 实现地址簿、默认地址和基于地址的下单。
- [x] 实现登录页、个人中心、地址管理和结算选址。
- [x] 完成自动化测试、构建、联调和视觉检查。

## 第四阶段决策

- 商品读取接口保持公开，购物车、订单、地址和账号接口必须认证。
- 保留 CSRF 防护；认证 Cookie 与 CSRF Token 分离。
- 下单请求只接受当前用户的 `addressId`，订单继续保存收货快照。
- 不使用用户提供的 API Key；本阶段不接真实支付。

## 第四阶段验证记录

- `mvn -q test` 通过：38 个测试，0 失败。
- `npm run build` 通过，TypeScript 与 Vite 生产构建成功。
- MySQL 8.0.31 实际联调通过；`store_users` 与 `store_addresses` 自动建表。
- 匿名购物车返回 401，无 CSRF Token 的写请求返回 403。
- `JSESSIONID` 包含 `HttpOnly; SameSite=Lax`，登录恢复、退出和退出后 401 均通过。
- 双账号隔离通过：账号 B 购物车为空，访问账号 A 的地址和订单均返回 404。
- BCrypt 摘要为 `$2a$` 前缀、60 字符；测试订单取消后库存已恢复。
- Edge 无头模式完成 1440x1000 桌面登录页与手机尺寸响应式视觉检查。
